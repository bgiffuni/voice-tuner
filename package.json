{
  "name": "voice-tuner",
  "version": "1.0.0",
  "description": "Voice Tuner — a mixing-console app for defining your writing voice. Ingest writing samples (paste, upload, or URL), let Claude auto-set the console, manage multiple named styles, and share Voice Cards.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.69.0",
    "express": "^4.21.2",
    "mammoth": "^1.12.0",
    "nodemailer": "^9.0.3",
    "pdf-parse": "^2.4.5",
    "tsx": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.0",
    "@types/nodemailer": "^8.0.1",
    "typescript": "^5.7.2"
  }
}
